/** MVP table economics — no buy-in flow yet (Phase 6C1). */

export const DEFAULT_STARTING_CHIPS = 200 as const;

/** MVP rebuy top-up for busted players (Phase 7G). */
export const DEFAULT_REBUY_CHIPS = 200 as const;

export const DEFAULT_SMALL_BLIND = 1 as const;

export const DEFAULT_BIG_BLIND = 2 as const;
